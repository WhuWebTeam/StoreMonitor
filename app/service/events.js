/**
 * enclosure of some database opration releated to table events
 * @module events
 * 
 * @file StoreMonitor
 * @version 0.0.1
 */

/** events */
module.exports = app => {
    /**
     * used to complete module evens' function
     * @class
     * @extends app.Service
     */
    class Events extends app.Service {
        
    }

    return Events;
}